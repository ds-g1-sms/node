# Chat Client

This directory contains the chat client application.

## Purpose

Simple client application for connecting to chat nodes and participating in chat rooms.

In the prototype, clients are co-located with servers on the same machines.

## Running

```bash
go run main.go
```
